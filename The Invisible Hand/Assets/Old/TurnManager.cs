/*
using UnityEngine;
using System.Collections;

//class that updates the turn counter
public class TurnManager : Singleton<TurnManager> {

  protected TurnManager() { }
  
  //variable that keeps track of the turn amount
  private int turn;

  //increments turn by one
  public void nextTurn() {
        turn++;
        UIManager.Instance.updateTurn();
  }
  //constructs the public variable Turn used outside of the class
  public int Turn { get { return turn; } }
}
*/
